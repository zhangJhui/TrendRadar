# TrendRadar API 接口设计方案

## 📋 设计原则

**核心原则：零侵入式设计**
- ❌ 不修改现有数据库结构
- ❌ 不修改现有业务逻辑
- ❌ 不添加用户系统和认证
- ✅ 纯粹的API接口层
- ✅ 复用现有数据和功能
- ✅ 完全向后兼容

## 🏗️ 整体架构

```
TrendRadar
├── 现有功能 (完全不变)
│   ├── 数据抓取 & 存储
│   ├── HTML报告生成
│   └── 通知推送
└── 新增API层 (纯接口)
    ├── FastAPI服务器
    ├── 读取现有数据
    └── 调用现有功能
```

## 📡 API 接口规范

### 1. 获取资讯接口

#### 基本信息
- **路径**: `POST /news-api/news`
- **描述**: 获取结构化的新闻数据
- **认证**: 暂无 (后续版本添加)

#### 请求体格式
```json
{
  "date": "2026-01-12",           // 可选，默认今天
  "platforms": ["zhihu", "weibo"], // 可选，默认全部平台
  "keywords": ["AI", "科技"],      // 可选，关键词过滤
  "limit": 50,                    // 可选，每个平台返回数量限制
  "mode": "current",              // 可选：current/daily/incremental
  "include_rss": true,            // 可选，是否包含RSS订阅内容
  "outputs": "json"               // 可选：json/html，默认json
}
```

#### 请求示例
```http
POST /news-api/news
Content-Type: application/json

{
  "date": "2026-01-12",
  "platforms": ["zhihu", "weibo"],
  "keywords": ["AI", "科技"],
  "limit": 20,
  "outputs": "json"
}
```

#### 响应格式

##### JSON格式 (outputs: "json")
```json
{
  "success": true,
  "timestamp": "2026-01-12T16:30:00+08:00",
  "request_id": "req_20260112_001",
  "data": {
    "platforms": {
      "知乎": {
        "platform_id": "zhihu",
        "platform_name": "知乎",
        "total_count": 15,
        "matched_count": 8,
        "news_list": [
          {
            "id": "zhihu_20260112_001",
            "title": "AI技术发展趋势分析",
            "url": "https://zhihu.com/question/xxx",
            "mobile_url": "https://zhihu.com/question/xxx",
            "rank": 3,
            "hot_score": 1250000,
            "first_seen": "2026-01-12T14:20:00+08:00",
            "last_seen": "2026-01-12T16:25:00+08:00",
            "keyword_matched": ["AI", "技术"],
            "is_new": true,
            "appear_count": 5,
            "rank_history": [5, 4, 3, 3]
          }
        ]
      },
      "微博": {
        "platform_id": "weibo",
        "platform_name": "微博",
        "total_count": 12,
        "matched_count": 6,
        "news_list": [...]
      }
    },
    "rss_feeds": {
      "Hacker News": {
        "feed_id": "hackernews",
        "feed_name": "Hacker News",
        "feed_url": "https://hnrss.org/frontpage",
        "total_count": 3,
        "matched_count": 2,
        "articles": [
          {
            "id": "hn_20260112_001",
            "title": "Don't fall into the anti-AI hype",
            "url": "https://antirez.com/news/158",
            "published_at": "2026-01-12T10:30:00+08:00",
            "author": "antirez",
            "keyword_matched": ["AI"],
            "is_fresh": true
          }
        ]
      }
    },
    "summary": {
      "total_platforms": 11,
      "active_platforms": 8,
      "total_news": 127,
      "matched_news": 35,
      "new_news": 8,
      "rss_articles": 5,
      "keywords_found": ["AI", "科技", "投资"],
      "date_range": {
        "start": "2026-01-12T00:00:00+08:00",
        "end": "2026-01-12T16:30:00+08:00"
      }
    }
  }
}
```

##### HTML格式 (outputs: "html")
当 `outputs` 设置为 `"html"` 时，接口直接返回HTML内容：

**响应头**：
```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8
Content-Length: 45678
```

**响应体**：
```html
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>热点新闻分析</title>
    <style>
        /* 完整的CSS样式 */
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="header-title">热点新闻分析</h1>
            <!-- 完整的HTML报告内容，包括AI分析 -->
        </div>
        <!-- ... 其他内容 ... -->
    </div>
</body>
</html>
```

**HTML输出特性**：
- 直接返回完整的HTML页面（与 index.html 相同）
- 包含完整的CSS样式和JavaScript功能
- 支持保存为图片功能
- 包含AI分析内容（如果启用）
- 响应式设计，支持移动端查看
- 可直接在浏览器中打开

**使用场景**：
- 前端iframe嵌入显示
- 浏览器直接访问查看
- 生成PDF报告（通过浏览器打印）
- 邮件发送HTML格式报告
- 微信小程序web-view组件显示

### 2. AI分析接口

#### 基本信息
- **路径**: `POST /news-api/analysis`
- **描述**: 对指定新闻进行AI分析
- **认证**: 暂无 (后续版本添加)

#### 请求体格式
```json
{
  "analysis_type": "news_ids",  // "news_ids" 或 "query"

  // 方式1: 指定新闻ID
  "news_ids": [
    "zhihu_20260112_001",
    "weibo_20260112_005"
  ],

  // 方式2: 使用查询条件
  "query": {
    "date": "2026-01-12",
    "platforms": ["zhihu", "weibo"],
    "keywords": ["AI"],
    "limit": 50
  },

  // 分析选项
  "options": {
    "include_rss": false,
    "max_news": 50,
    "language": "zh-CN",
    "analysis_depth": "standard",  // "quick", "standard", "deep"
    "custom_prompt": ""  // 可选的自定义分析提示词
  }
}
```

#### 响应格式
```json
{
  "success": true,
  "timestamp": "2026-01-12T16:30:00+08:00",
  "request_id": "req_20260112_002",
  "analysis_id": "analysis_20260112_001",
  "data": {
    "analysis": {
      "summary": "当前热点聚焦于中美关系动态、国内金融政策及AI技术应用。美国撤回无人机限制、特朗普涉台言论引发关注，同时中国ETF大额分红与AI投资热潮成为财经焦点。",
      "keyword_analysis": "中美议题热度稳定持久，多话题出现10次且排名靠前。AI与商业航天话题波动较大，显示市场关注度高但观点分化。体育新闻热度较低且短暂。",
      "sentiment": "公众对中美关系议题情感复杂，涉及台湾问题的表态引发震动与担忧；对AI技术发展普遍持积极与好奇态度，但对经济冲击存在辩论；金融政策消息以中性解读为主。",
      "cross_platform": "中美关系、AI、商业航天话题在财经、新闻及社交平台广泛同步，显示其宏观影响力。社会性议题（如美国示威）主要在社交平台发酵。",
      "impact": "中美政策动向可能影响双边经贸与科技合作氛围。AI与能源结合、商业航天发展信号强劲，预示相关产业链将获持续关注与投资。",
      "signals": "Meta投资核电厂为AI供电话题出现较晚但迅速上榜，显示\"AI+能源\"成为新焦点。美国司法部调查美联储主席属罕见政治事件，值得警惕。",
      "conclusion": "建议投资者关注AI向能源与航天等实体产业融合的应用机会。政策制定者需密切关注中美科技博弈下的产业链反馈。"
    },
    "metadata": {
      "total_news": 37,
      "analyzed_news": 35,
      "hotlist_count": 35,
      "rss_count": 2,
      "platforms_analyzed": ["知乎", "微博", "百度热搜", "财联社"],
      "keywords_analyzed": ["AI", "中美关系", "投资", "商业航天"],
      "analysis_time": "2026-01-12T16:30:00+08:00",
      "processing_duration": "12.5s",
      "model_info": {
        "provider": "custom",
        "model": "deepseek-v3-2-251201",
        "api_endpoint": "https://ark.cn-beijing.volces.com/api/v3/chat/completions"
      }
    },
    "news_analyzed": [
      {
        "id": "zhihu_20260112_001",
        "title": "AI技术发展趋势分析",
        "platform": "知乎",
        "weight": 0.85
      }
    ]
  }
}
```

## 🛠️ 技术实现方案

### 1. 技术栈选择

#### API框架
- **FastAPI** - 轻量、快速、自动文档生成

#### 数据来源
- **复用现有SQLite数据库** - 不修改表结构
- **读取现有output目录** - 获取HTML和数据文件
- **调用现有函数** - 复用HTML生成和AI分析逻辑

#### 数据ID生成规则
```python
# 使用现有数据库的rowid或组合字段生成
# 格式: {platform}_{timestamp}_{hash}
# 示例: zhihu_20260112160000_abc123

def generate_news_id(platform_id: str, title: str, timestamp: str) -> str:
    import hashlib
    hash_str = hashlib.md5(f"{platform_id}_{title}".encode()).hexdigest()[:6]
    return f"{platform_id}_{timestamp}_{hash_str}"
```

**重要**：不修改现有数据库，ID仅用于API响应

### 2. 项目结构

```
trendradar/
├── api/                    # 新增API模块
│   ├── __init__.py
│   ├── app.py             # FastAPI应用
│   ├── routes/            # 路由模块
│   │   ├── __init__.py
│   │   ├── news.py        # 资讯接口
│   │   └── analysis.py    # AI分析接口
│   ├── models/            # 数据模型
│   │   ├── __init__.py
│   │   ├── request.py     # 请求模型
│   │   └── response.py    # 响应模型
│   ├── services/          # 业务逻辑
│   │   ├── __init__.py
│   │   ├── news_service.py
│   │   └── analysis_service.py
│   └── utils/             # 工具函数
│       ├── __init__.py
│       ├── auth.py        # 认证
│       └── formatters.py  # 数据格式化
├── 现有模块...
```

### 3. 启动方式

#### 开发模式
```bash
# 启动API服务器
python -m trendradar.api --host 0.0.0.0 --port 8000

# 或使用uvicorn
uvicorn trendradar.api.app:app --host 0.0.0.0 --port 8000 --reload

# API访问地址
# 获取资讯: POST http://localhost:8000/news-api/news
# AI分析: POST http://localhost:8000/news-api/analysis
```

#### 生产模式
```bash
# 使用gunicorn
gunicorn trendradar.api.app:app -w 4 -k uvicorn.workers.UvicornWorker
```

## 🔐 后续功能规划

### 认证系统 (v2.0)

后续版本将添加简单的Token认证：

```yaml
# 未来的配置方式
api:
  enabled: true
  access_token: "your_token_here"  # 可选
```

### 其他规划功能
- 请求速率限制
- 访问日志记录
- API使用统计
- 缓存优化

## 📊 性能优化

### 1. 缓存策略
- Redis缓存热门查询结果
- 新闻数据缓存5分钟
- AI分析结果缓存1小时

### 2. 数据库优化
- 添加必要的索引
- 查询结果分页
- 连接池管理

## 📚 API文档

### 自动生成文档
FastAPI自动生成的交互式API文档：
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

### 错误码定义
| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 429 | 请求过于频繁 |
| 500 | 服务器内部错误 |

## 🚀 部署方案

### 1. Docker部署
```dockerfile
# 扩展现有Dockerfile
EXPOSE 8000
CMD ["uvicorn", "trendradar.api.app:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 2. 反向代理
```nginx
# Nginx配置
location /news-api/ {
    proxy_pass http://localhost:8000/news-api/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

## 🧪 测试计划

### 1. 单元测试
- 数据模型验证
- 业务逻辑测试
- API接口测试

### 2. 集成测试
- 端到端API测试
- 性能压力测试
- 安全性测试

## 📈 监控指标

### 1. 业务指标
- API调用次数
- 响应时间
- 错误率
- AI分析成功率

### 2. 技术指标
- 内存使用率
- CPU使用率
- 数据库连接数
- 缓存命中率

---

## 💡 实施建议

### 核心原则：零侵入实现

1. **纯接口层**：
   - 只添加 `trendradar/api/` 模块
   - 不修改任何现有代码
   - 不修改数据库结构

2. **数据复用策略**：
   - 读取现有SQLite数据库
   - 调用现有的HTML生成函数
   - 调用现有的AI分析函数
   - 使用现有的配置系统

3. **分阶段实施**：
   - Phase 1: 基础API框架 + 资讯获取接口 (JSON)
   - Phase 2: HTML格式输出（调用现有HTML生成）
   - Phase 3: AI分析接口（调用现有AI分析）

4. **测试策略**：
   - API接口单独测试
   - 不影响现有功能测试
   - 可以独立开关API服务

### 实现要点

- **启动方式**：`python -m trendradar.api` (独立启动)
- **数据源**：现有SQLite文件和output目录
- **配置**：复用现有config.yaml，可选添加api配置段
- **依赖**：只添加FastAPI相关依赖

这样确保现有功能100%不受影响，API是纯粹的附加功能。